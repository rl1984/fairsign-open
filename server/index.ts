// Open Source Edition entry point
// This file starts the base server without Enterprise features
// For Enterprise Edition, build uses src/index-ee.ts as the entry point
import { startServer } from "../src/index";

startServer();
